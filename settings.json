{
  "css.validate": false,
  "tailwindCSS.experimental.classRegex": [
    "tw`([^`]*)", // tw`...`
    "tw=\"([^\"]*)", // tw="..."
    "tw={\"([^\"}]*)", // tw={"..."}
    "className=\"([^\"]*)", // className="..."
    "className={`([^`]*)`" // className={`...`}
  ]
}
